### Airtable project with typescript, airtable API, cypress and react-redux

#### To run
type `npm install` and then `npm start` in a terminal

### To run cypress
type `npx cypress open` and then navigate the pop